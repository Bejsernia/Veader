import React, { useEffect, useState } from 'react';
import { ActivityIndicator, ScrollView, Text, View } from 'react-native';
import { Ionicons } from '@expo/vector-icons';
import { SafeAreaView } from 'react-native-safe-area-context';
import type { ReadingStatsRange, ReadingStatsSummary } from '../../domain/models';
import { statsRepository } from '../../data/stats-repository';
import { PressableScale } from '../../ui/components/pressable-scale';
import { useTheme } from '../../ui/theme';
import { styles, layoutStyles, pageLayoutStyles, uiStyles } from '../../ui/legacy-styles';
import { IconButton } from '../shared/library-ui';

function duration(value: number) { const minutes = Math.round(value / 60000); return minutes >= 60 ? `${Math.floor(minutes / 60)}小时 ${minutes % 60}分` : `${minutes}分钟`; }

function BarList({ rows, isDark, formatValue = duration }: { rows: Array<{ name: string; value: number; detail: string }>; isDark: boolean; formatValue?: (value: number) => string }) {
  const max = Math.max(1, ...rows.map(row => row.value));
  return <View style={{ gap: 13 }}>{rows.length ? rows.map(row => <View key={row.name}><View style={{ flexDirection: 'row', justifyContent: 'space-between', gap: 12 }}><Text numberOfLines={1} style={[styles.rowTitle, { flex: 1 }, isDark && styles.textPrimaryDark]}>{row.name}</Text><Text style={[styles.meta, isDark && styles.textMutedDark]}>{formatValue(row.value)}</Text></View><View style={{ height: 7, borderRadius: 4, overflow: 'hidden', backgroundColor: isDark ? '#3A3443' : '#E3E0E7', marginTop: 5 }}><View style={{ height: '100%', width: `${Math.max(3, row.value / max * 100)}%`, borderRadius: 4, backgroundColor: '#7257E7' }} /></View><Text style={[styles.meta, { fontSize: 11 }, isDark && styles.textMutedDark]}>{row.detail}</Text></View>) : <Text style={[styles.meta, isDark && styles.textMutedDark]}>暂无数据</Text>}</View>;
}

function DailyBars({ summary, isDark }: { summary: ReadingStatsSummary; isDark: boolean }) {
  const rows = summary.daily.slice(-30);
  const max = Math.max(1, ...rows.map(item => item.durationMs));
  return <View style={{ height: 150, flexDirection: 'row', alignItems: 'flex-end', gap: 5 }}>{rows.length ? rows.map(item => <View key={item.key} style={{ flex: 1, height: '100%', alignItems: 'center', justifyContent: 'flex-end', gap: 5 }}><View style={{ width: '72%', minHeight: 4, height: `${Math.max(3, item.durationMs / max * 100)}%`, borderRadius: 5, backgroundColor: '#0F9F91' }} /><Text style={[{ color: isDark ? '#B8B1C2' : '#8A8691', fontSize: 9 }, rows.length > 14 && { transform: [{ rotate: '-45deg' }] }]}>{item.label}</Text></View>) : <Text style={[styles.meta, isDark && styles.textMutedDark]}>开始阅读后这里会显示每日趋势</Text>}</View>;
}

export function ReadingStatsScreen({ back }: { back: () => void }) {
  const { isDark } = useTheme();
  const [range, setRange] = useState<ReadingStatsRange>('7d');
  const [summary, setSummary] = useState<ReadingStatsSummary>();
  const [loading, setLoading] = useState(true);
  useEffect(() => { let active = true; setLoading(true); void statsRepository.getSummary(range).then(value => { if (active) setSummary(value); }).catch(console.warn).finally(() => active && setLoading(false)); return () => { active = false; }; }, [range]);
  return <SafeAreaView style={[styles.safe, isDark && styles.safeDark]}><ScrollView contentContainerStyle={[styles.page, isDark && styles.pageDark]}><View style={[styles.header, layoutStyles.subpageHeader]}><IconButton name="chevron-back" label="返回最近阅读" onPress={back} /><Text style={[styles.navTitle, isDark && styles.textPrimaryDark]}>阅读统计</Text><View style={pageLayoutStyles.headerSpacer} /></View><View style={[styles.segment, isDark && uiStyles.segmentDark]}>{(['7d', '30d', 'all'] as ReadingStatsRange[]).map(value => <PressableScale key={value} onPress={() => setRange(value)} style={[styles.segmentItem, range === value && styles.segmentActive, isDark && range === value && uiStyles.segmentActiveDark]}><Text style={[styles.segmentText, range === value && styles.segmentTextActive, isDark && value !== range && uiStyles.segmentTextDark]}>{value === '7d' ? '7天' : value === '30d' ? '30天' : '全部'}</Text></PressableScale>)}</View>{loading || !summary ? <View style={uiStyles.loadingState}><ActivityIndicator color="#7257E7" /><Text style={[styles.meta, isDark && styles.textMutedDark]}>正在计算阅读数据…</Text></View> : <><View style={styles.statsOverviewGrid}><View style={[styles.statsOverviewCard, isDark && styles.cardDark]}><Ionicons name="time-outline" size={20} color="#7257E7" /><Text style={[styles.statsOverviewValue, isDark && styles.textPrimaryDark]}>{duration(summary.totalDurationMs)}</Text><Text style={[styles.meta, isDark && styles.textMutedDark]}>阅读时长</Text></View><View style={[styles.statsOverviewCard, isDark && styles.cardDark]}><Ionicons name="book-outline" size={20} color="#0F9F83" /><Text style={[styles.statsOverviewValue, isDark && styles.textPrimaryDark]}>{summary.totalPages}</Text><Text style={[styles.meta, isDark && styles.textMutedDark]}>阅读页数</Text></View><View style={[styles.statsOverviewCard, isDark && styles.cardDark]}><Ionicons name="library-outline" size={20} color="#A855F7" /><Text style={[styles.statsOverviewValue, isDark && styles.textPrimaryDark]}>{summary.bookCount}</Text><Text style={[styles.meta, isDark && styles.textMutedDark]}>阅读作品</Text></View><View style={[styles.statsOverviewCard, isDark && styles.cardDark]}><Ionicons name="checkmark-circle-outline" size={20} color="#C56B1C" /><Text style={[styles.statsOverviewValue, isDark && styles.textPrimaryDark]}>{summary.completedChapterCount}</Text><Text style={[styles.meta, isDark && styles.textMutedDark]}>完成章节</Text></View></View><View style={[styles.statsPanel, isDark && styles.cardDark]}><Text style={[styles.sectionTitle, isDark && styles.textPrimaryDark]}>每日阅读时长</Text><DailyBars summary={summary} isDark={isDark} /></View><View style={[styles.statsPanel, isDark && styles.cardDark]}><Text style={[styles.sectionTitle, isDark && styles.textPrimaryDark]}>按作品</Text><BarList isDark={isDark} rows={summary.byBook.map(row => ({ name: row.title, value: row.durationMs, detail: `${row.pages} 页 · ${Math.round(row.progress * 100)}%`, }))} /></View><View style={[styles.statsPanel, isDark && styles.cardDark]}><Text style={[styles.sectionTitle, isDark && styles.textPrimaryDark]}>按 tag</Text><BarList isDark={isDark} rows={summary.byTag.map(row => ({ name: row.name, value: row.durationMs, detail: `${row.pages} 页`, }))} /></View><View style={[styles.statsPanel, isDark && styles.cardDark]}><Text style={[styles.sectionTitle, isDark && styles.textPrimaryDark]}>按作者</Text><BarList isDark={isDark} rows={summary.byAuthor.map(row => ({ name: row.name, value: row.durationMs, detail: `${row.pages} 页`, }))} /></View><View style={[styles.statsPanel, isDark && styles.cardDark]}><Text style={[styles.sectionTitle, isDark && styles.textPrimaryDark]}>按格式</Text><BarList isDark={isDark} rows={summary.byFormat.map(row => ({ name: row.format.toUpperCase(), value: row.durationMs, detail: `${row.pages} 页`, }))} /></View></>}</ScrollView></SafeAreaView>;
}
